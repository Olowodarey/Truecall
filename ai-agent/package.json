{
  "name": "truecall-ai-agent",
  "version": "1.0.0",
  "description": "TrueCall AI Agent — watches for matches added on-chain, fetches verified scores from API-Football, and submits results back to the EventManager contract",
  "main": "dist/index.js",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "lint": "eslint src --ext .ts"
  },
  "dependencies": {
    "axios": "^1.7.2",
    "dotenv": "^16.4.5",
    "viem": "^2.17.0",
    "winston": "^3.13.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "tsx": "^4.15.0",
    "typescript": "^5.4.5"
  }
}
